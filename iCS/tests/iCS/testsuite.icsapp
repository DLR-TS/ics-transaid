# testing meta processing
meta

# basic tests
basic

# basic tests employing lightcomm instead of ns3
basic_lightcomm
bugs

# Tests for the demo app provided by the iTETRIS 2.0 release by EURECOM.
# To run these tests, disable parallel test runs in tab "Running" -> check "Run tests sequentially"
traffic_monitor_(no_parallel_runs!)
